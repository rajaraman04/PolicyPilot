# --- Core API ---
fastapi
uvicorn[standard]
pydantic
pydantic-settings

# --- Agentic RAG ---
langgraph
langchain
langchain-community
langchain-openai        # default provider: embeddings + LLM client
langchain-anthropic     # optional provider: set LLM_PROVIDER=anthropic to use
langchain-huggingface   # local embeddings integration
sentence-transformers   # local (no-key) embedding models — default
chromadb

# --- PDF ingestion ---
pypdf

# --- UI ---
streamlit

# --- Config / utils ---
python-dotenv
httpx                   # Streamlit -> FastAPI calls

# --- Tests ---
pytest
